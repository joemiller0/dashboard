import { useState } from "react";
import "../stylesheets/workout.css";

const Workout = (props) => {

    return (
        <div className="workout">
            <p>test</p>
        </div>);
};

export default Workout;
